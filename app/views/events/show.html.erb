
<p>
  <strong>Title:</strong>
  <%= @event.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @event.description %>
</p>

<p>
  <strong>Event date:</strong>
  <%= @event.date %>
</p>

  <% if @not_registered and current_user %>
    <h3>
    <a href=<%= register_event_path(id: params[:id]) %> role="button">Register Now</a> 
    </h3>
  <% elsif current_user %>
    <h4>
    You already Registered for this event
    </h4>
  <% end %>
<%= link_to 'Home', events_path %>

<h2> Event Attendees (<%= @event.attendee_ids.count%>) </h2>

<ul>
  <% @users.each do |user| %>
   <li> <%= user.username %> </li>  
  <% end %> 
</ul>
  
